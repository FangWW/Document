1。请写一段代码，读出窗体上所有的控件，并显示控件的名称。
2。在ASP.NET中，如何强制要求来访都进行windows集成身份验证。
3。有一个需求，希望从SQL SERVER数据库中读出一条记录，并写到一个文本文件中，请写出需要using的命名空间。
4。二进制01000001等于十进制的_______,十六进制的101等于十进制的________。
5。写一段代码对变量进行拆/装箱操作。二1。系统有三个关联表：表A中有字段（no=记录名，主键。u＝用户名）。表B中有字段（noo=记录号，主键，T＝教师名，K=科目名称）。表C中有字段（Sno=记录号，Cno＝表A的外键，Cnoo＝表B的外键，C=用户成绩）。问题1：写一段TSQL语句，查出教师名中姓“张”的科目中成绩低于60分的用户，要求输出用户名并根据成绩排序。问题2：写一段TSQL语句，列出以教师名进行排序的，并进行分组，以60分以下为一组，60－70一组，70－80一组，80－90一组，90－100一组，要求输出用户名称，科目名称。2。已知表A中字段（username,logtime），表B中有字段（username,password,logintotal）写个触发器，当表B的logintotal值发生变化时，表A中添加一条记录。 